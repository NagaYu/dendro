# Hugging Face Space runtime dependencies.
# The Dendro core needs only numpy + scipy; the Space adds gradio, matplotlib
# (for the evidence timeline) and requests (for the archive APIs).
numpy>=1.24
scipy>=1.10
requests>=2.28
matplotlib>=3.7
gradio>=5.0
